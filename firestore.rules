rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development/testing only.
    // Replace before production with authenticated, landlord-scoped rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

# Web App

Phase 1 reserves the frontend feature boundaries. Implementation starts after backend authentication and tenant context are in place.

- `app`: providers, app shell, route wiring
- `components`: shared UI components
- `features`: domain features
- `lib`: API client, utilities, auth helpers
- `routes`: route definitions
- `styles`: Tailwind and theme variables
